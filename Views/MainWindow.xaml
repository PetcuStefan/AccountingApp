<Window x:Class="AccountingApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:AccountingApp.ViewModels"
        Title="AccountingApp - Reports"
        Height="500" Width="900"
        Background="#fafafa"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page title -->
        <TextBlock Text="Raportari"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#333"
                   Margin="0,0,0,20" Grid.Row="0"/>

        <!-- Side-by-side Costs and Revenues -->
        <Grid Grid.Row="1">
            <!-- Use a spacer column instead of ColumnSpacing -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <!-- spacer column -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Costs Table (left column) -->
            <StackPanel Grid.Column="0">
                <TextBlock Text="Costs"
               FontSize="18"
               FontWeight="SemiBold"
               Foreground="#b71c1c"
               Margin="0,0,0,10"/>
                <DataGrid ItemsSource="{Binding Costs}"
              Style="{StaticResource CostDataGridStyle}"
              AutoGenerateColumns="False" Height="250">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID"   Binding="{Binding id}"    Width="Auto"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding value}" Width="*"/>
                        <DataGridTextColumn Header="Path" Binding="{Binding path}"  Width="2*"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding date}"  Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Add Cost Button -->
                <Button Content="Add Cost"
            Margin="0,10,0,0"
            Background="#e57373"
            Foreground="White"
            Padding="6"
            Command="{Binding AddCostCommand}"/>
            </StackPanel>


            <!-- Revenues Table (right column) -->
            <StackPanel Grid.Column="2">
                <TextBlock Text="Revenues"
               FontSize="18"
               FontWeight="SemiBold"
               Foreground="#1b5e20"
               Margin="0,0,0,10"/>
                <DataGrid ItemsSource="{Binding Revenues}"
              Style="{StaticResource RevenueDataGridStyle}"
              AutoGenerateColumns="False" Height="250">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID"   Binding="{Binding id}"    Width="Auto"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding value}" Width="*"/>
                        <DataGridTextColumn Header="Path" Binding="{Binding path}"  Width="2*"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding date}"  Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Add Revenue Button -->
                <Button Content="Add Revenue"
            Margin="0,10,0,0"
            Background="#81c784"
            Foreground="White"
            Padding="6"
            Command="{Binding AddRevenueCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
