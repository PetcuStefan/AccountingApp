<Window x:Class="AccountingApp.Views.AddReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Report"
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Value -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <TextBlock Text="Value:" Width="100" VerticalAlignment="Center"/>
            <TextBox x:Name="ValueBox" Width="200"/>
        </StackPanel>

        <!-- Date -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 0 0 10">
            <TextBlock Text="Date:" Width="100" VerticalAlignment="Center"/>
            <DatePicker x:Name="DatePicker" Width="200"/>
        </StackPanel>

        <!-- File Drop / Click Zone -->
        <Border Grid.Row="2"
                Margin="0 0 0 10"
                CornerRadius="8"
                BorderBrush="#4CAF50"
                BorderThickness="2"
                Background="#F0FFF4"
                AllowDrop="True"
                DragEnter="FileDropZone_DragEnter"
                DragLeave="FileDropZone_DragLeave"
                Drop="FileDropZone_Drop"
                MouseLeftButtonUp="FileDropZone_Click"
                Cursor="Hand"
                Padding="20"
                x:Name="FileDropZone">

            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Click or Drop a JPG, PNG, or PDF file here"
                           FontSize="14"
                           Foreground="#444"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="FileNameText"
                           Text="No file selected"
                           FontStyle="Italic"
                           Foreground="#888"
                           Margin="0 5 0 0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center">
            <Button Content="Save" Width="80" Height="25" Margin="0 0 10 0" Click="Save_Click"/>
            <Button Content="Cancel" Width="80" Height="25" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
